/** @type {import('next').NextConfig} */
const nextConfig = {
  // Permite importar JSON desde public/data en el servidor
  experimental: {},
  // Vercel no necesita configuración especial de output
};

export default nextConfig;
